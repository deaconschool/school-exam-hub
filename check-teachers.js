import { serviceClient } from './src/services/supabaseService'; import { supabaseUrl } from './src/services/supabaseService'; console.log('Supabase URL:', supabaseUrl); console.log('Service client available:', \!\!serviceClient); serviceClient.from('teachers').select('id, name, is_active').then(result => console.log('Teachers:', result)).catch(err => console.error('Error:', err));
