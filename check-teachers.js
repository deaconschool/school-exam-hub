import { serviceClient } from './src/services/supabaseService'; import { supabaseUrl } from './src/services/supabaseService'; serviceClient.from('teachers').select('id, name, is_active').then(result => {}).catch(err => {});
